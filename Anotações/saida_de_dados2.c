#include <stdio.h>
int main()
{

    printf("Hoje está um \"LINDO\" dia !!!\n");  // CAracterec especial \ é utilizada para retirar o siginificado especial que um caractere possui no caso ("") não é possível adicionar as sendo que elas delimitam uma string.//

return 0;

}
                                                // Em alguns casos serve parar representar alguns caracteres como://
                                                // \n(New Line)// 
                                                // \7(Bell)//
                                                // \a(Bell)//
                                                // \b(BackSpace)//
                                                // \r(Carriage Return)//
                                                // \t(Tabulação Vertical)//
                                                // \\(CAracterer[representa a próŕia \])//
                                                // \'(aspas simples ')//
                                                // \"(aspas)//
                                                // \?(ponto de interrogação)