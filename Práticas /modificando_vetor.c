#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i, n;
    int vetor[5];

    for ( i = 0; i < 5; i++)
    {
        printf("Digite um número: ");
        scanf("%d", &vetor[i]);
        }
    printf("Digite o valor que deseja excluir: ");
    scanf("%n", &n);
    for ( i = 0; i < 5; i++)
    {   if (vetor[i] == n)
    {
        
    }
    
            
            }
    
    


}